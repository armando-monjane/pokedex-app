import styled from '@mui/material/styles/styled';
import Box from '@mui/material/Box';

export const PokemonListItem = styled(Box)(() => ({
    display: 'flex',
    flexDirection: 'row',
    width: '100%',
    alignItems: 'center',
}));